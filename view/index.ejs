<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Catálogo de Livros</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Catálogo de Livros</h1>
    <p class="book-count">Exibindo de <%= start %> até <%= end %> de <%= count %> livros</p>
    <table>
      <thead>
        <tr>
          <th>Título</th>
          <th>Autor</th>
          <th>ISBN</th>
          <th>Páginas</th>
          <th>Ano</th>
          <th>Valor</th>
        </tr>
      </thead>
      <tbody>
        <% books.forEach(book => { %>
          <tr>
            <td><%= book.titulo %></td>
            <td><%= book.autor %></td>
            <td><%= book.isbn %></td>
            <td><%= book.paginas %></td>
            <td><%= book.ano %></td>
            <td>R$ <%= book.valor.toFixed(2) %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <div class="pagination">
      <a href="?page=1">&laquo;&laquo;</a>
      <a href="?page=<%= current - 1 < 1 ? 1 : current - 1 %>">&laquo;</a>
      
      <% for (let i = Math.max(1, current - 3); i <= Math.min(pages, current + 3); i++) { %>
        <a href="?page=<%= i %>" class="<%= i === current ? 'active' : '' %>"><%= i %></a>
      <% } %>
      
      <a href="<%= current < pages ? '?page=' + (current + 1) : '#' %>">&raquo;</a>
      <a href="?page=<%= pages %>">&raquo;&raquo;</a>
    </div>
  </div>
</body>
</html>
